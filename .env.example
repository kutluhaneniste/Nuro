# Bu dosyayı .env olarak kopyala ve doldur:
#   cp .env.example .env

# Modal deploy çıktısı (sonunda / olmasın)
TRIBE_API_URL=https://SENIN-WORKSPACE--tribe-v2.modal.run

# Modal secret'ta TRIBE_API_KEY tanımlıysa buraya da yaz (yoksa boş bırak)
# TRIBE_API_KEY=
